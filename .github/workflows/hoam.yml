name: cron

on:
  schedule:
    - cron: "0 1,13 * * *"
  workflow_dispatch:

jobs:
  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: "pip"

      - run: pip install -r requirements.txt
        working-directory: ./hoam

      - run: >
          curl --location 'https://hoamwedding.com/wp-admin/admin-ajax.php' \
                --header 'Content-Type: application/x-www-form-urlencoded' \
          --header 'Cookie: PHPSESSID=if1at24tl85779f9t96rrgi4q2; mphb_session=1efb3511e4bc065eb2e4d33f9916b423%7C%7C1721226936%7C%7C1721226576' \
            --data-urlencode 'action=booked_calendar_date' \
            --data-urlencode 'date=2025-03-09' \
            --data-urlencode 'calendar_id=0'

      - id: main
        run: python hoam.py >> "$GITHUB_OUTPUT"
        working-directory: ./hoam

      - name: create an issue
        uses: dacbd/create-issue-action@main
        with:
          token: ${{ github.token }}
          title: Hoam Result
          body: ${{join(steps.main.outputs.*, '\n')}}
